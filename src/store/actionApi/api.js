import { createAction } from '@reduxjs/toolkit';
export const apiCalling = createAction('apiCalling');
export const apiSuccess = createAction('apiSuccess');
export const apiFailed = createAction('apiFailed');
